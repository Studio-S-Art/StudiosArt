<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Studio's Salon Art | Style by Professionals</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@300;400;500;600;700&family=Playfair+Display:wght@400;500;600&display=swap" rel="stylesheet">
    <style>
        :root {
            --primary: #8a6d5b;
            --primary-dark: #6d5546;
            --secondary: #f5e8dd;
            --accent: #d4b8a7;
            --dark: #333333;
            --light: #f9f5f2;
            --white: #ffffff;
            --gray: #777777;
            --success: #4CAF50;
            --transition: all 0.3s ease;
        }
        
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        
        body {
            font-family: 'Montserrat', sans-serif;
            line-height: 1.6;
            color: var(--dark);
            background-color: var(--white);
        }
        
        h1, h2, h3, h4, h5 {
            font-family: 'Playfair Display', serif;
            font-weight: 600;
            line-height: 1.3;
        }
        
        .container {
            width: 100%;
            max-width: 1200px;
            margin: 0 auto;
            padding: 0 20px;
        }
        
        /* Header & Navigation */
        header {
            background-color: var(--white);
            box-shadow: 0 2px 15px rgba(0,0,0,0.05);
            position: fixed;
            width: 100%;
            z-index: 1000;
        }
        
        .header-container {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 15px 20px;
        }
        
        .logo {
            display: flex;
            align-items: center;
            gap: 10px;
        }
        
        .logo h1 {
            font-size: 1.8rem;
            color: var(--primary);
        }
        
        .logo span {
            color: var(--accent);
            font-weight: 400;
        }
        
        nav ul {
            display: flex;
            list-style: none;
            gap: 30px;
        }
        
        nav a {
            text-decoration: none;
            color: var(--dark);
            font-weight: 500;
            font-size: 0.95rem;
            transition: var(--transition);
            padding: 5px 0;
            position: relative;
        }
        
        nav a:hover, nav a.active {
            color: var(--primary);
        }
        
        nav a::after {
            content: '';
            position: absolute;
            bottom: 0;
            left: 0;
            width: 0;
            height: 2px;
            background-color: var(--primary);
            transition: var(--transition);
        }
        
        nav a:hover::after, nav a.active::after {
            width: 100%;
        }
        
        .mobile-menu-btn {
            display: none;
            background: none;
            border: none;
            font-size: 1.5rem;
            color: var(--primary);
            cursor: pointer;
        }
        
        .phone-cta {
            background-color: var(--primary);
            color: var(--white);
            padding: 10px 15px;
            border-radius: 4px;
            font-weight: 600;
            text-decoration: none;
            transition: var(--transition);
            display: flex;
            align-items: center;
            gap: 8px;
        }
        
        .phone-cta:hover {
            background-color: var(--primary-dark);
        }
        
        /* Social Media in Header */
        .social-header {
            display: flex;
            align-items: center;
            gap: 15px;
            margin-left: 20px;
        }
        
        .social-header a {
            color: var(--primary);
            font-size: 1.2rem;
            transition: var(--transition);
        }
        
        .social-header a:hover {
            color: var(--accent);
            transform: translateY(-2px);
        }
        
        /* Hero Slider Section */
        .hero-slider {
            padding-top: 100px;
            position: relative;
            height: 80vh;
            min-height: 600px;
            overflow: hidden;
        }
        
        .slider-container {
            width: 100%;
            height: 100%;
            position: relative;
        }
        
        .slider-slide {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            opacity: 0;
            transition: opacity 1s ease-in-out;
            display: flex;
            align-items: center;
            justify-content: center;
        }
        
        .slider-slide.active {
            opacity: 1;
        }
        
        .slider-image {
            width: 100%;
            height: 100%;
            object-fit: cover;
            position: absolute;
            top: 0;
            left: 0;
        }
        
        .slider-video {
            width: 100%;
            height: 100%;
            object-fit: cover;
            position: absolute;
            top: 0;
            left: 0;
        }
        
        .slider-overlay {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: linear-gradient(rgba(0,0,0,0.5), rgba(0,0,0,0.3));
        }
        
        .slider-content {
            position: relative;
            z-index: 2;
            text-align: center;
            color: var(--white);
            max-width: 800px;
            padding: 0 20px;
            margin-top: 60px;
        }
        
        .slider-content h2 {
            font-size: 3.5rem;
            margin-bottom: 20px;
            text-shadow: 2px 2px 4px rgba(0,0,0,0.5);
        }
        
        .slider-content p {
            font-size: 1.3rem;
            margin-bottom: 30px;
            opacity: 0.9;
            text-shadow: 1px 1px 2px rgba(0,0,0,0.5);
        }
        
        .slider-nav {
            position: absolute;
            bottom: 30px;
            left: 0;
            width: 100%;
            display: flex;
            justify-content: center;
            align-items: center;
            gap: 20px;
            z-index: 3;
        }
        
        .slider-dots {
            display: flex;
            gap: 10px;
        }
        
        .slider-dot {
            width: 12px;
            height: 12px;
            border-radius: 50%;
            background-color: rgba(255,255,255,0.5);
            border: none;
            cursor: pointer;
            transition: var(--transition);
        }
        
        .slider-dot.active {
            background-color: var(--accent);
            transform: scale(1.2);
        }
        
        .slider-arrows {
            display: flex;
            gap: 15px;
        }
        
        .slider-arrow {
            background-color: rgba(255,255,255,0.2);
            color: var(--white);
            width: 40px;
            height: 40px;
            border-radius: 50%;
            border: none;
            display: flex;
            align-items: center;
            justify-content: center;
            cursor: pointer;
            font-size: 1.2rem;
            transition: var(--transition);
            backdrop-filter: blur(5px);
        }
        
        .slider-arrow:hover {
            background-color: var(--accent);
            transform: scale(1.1);
        }
        
        .slider-autoplay {
            position: absolute;
            right: 30px;
            bottom: 30px;
            z-index: 3;
        }
        
        .autoplay-btn {
            background-color: rgba(255,255,255,0.2);
            color: var(--white);
            border: none;
            width: 40px;
            height: 40px;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            cursor: pointer;
            font-size: 1.2rem;
            transition: var(--transition);
            backdrop-filter: blur(5px);
        }
        
        .autoplay-btn:hover {
            background-color: var(--accent);
        }
        
        .cta-button {
            display: inline-block;
            background-color: var(--accent);
            color: var(--dark);
            padding: 15px 35px;
            border-radius: 4px;
            text-decoration: none;
            font-weight: 600;
            font-size: 1.1rem;
            transition: var(--transition);
            margin: 10px;
        }
        
        .cta-button:hover {
            background-color: var(--white);
            transform: translateY(-3px);
        }
        
        .cta-secondary {
            background-color: transparent;
            border: 2px solid var(--accent);
            color: var(--accent);
        }
        
        .cta-secondary:hover {
            background-color: var(--accent);
            color: var(--dark);
        }
        
        /* Promotions Slider */
        .promotions {
            padding: 80px 0;
            background-color: var(--light);
        }
        
        .section-title {
            text-align: center;
            margin-bottom: 50px;
        }
        
        .section-title h2 {
            font-size: 2.5rem;
            color: var(--primary);
            position: relative;
            display: inline-block;
            padding-bottom: 15px;
        }
        
        .section-title h2::after {
            content: '';
            position: absolute;
            bottom: 0;
            left: 50%;
            transform: translateX(-50%);
            width: 80px;
            height: 3px;
            background-color: var(--accent);
        }
        
        .promo-slider {
            display: flex;
            gap: 30px;
            overflow-x: auto;
            padding: 20px 10px;
            scrollbar-width: thin;
        }
        
        .promo-slider::-webkit-scrollbar {
            height: 6px;
        }
        
        .promo-slider::-webkit-scrollbar-thumb {
            background-color: var(--accent);
            border-radius: 10px;
        }
        
        .promo-card {
            min-width: 300px;
            background-color: var(--white);
            border-radius: 8px;
            overflow: hidden;
            box-shadow: 0 5px 15px rgba(0,0,0,0.08);
            transition: var(--transition);
        }
        
        .promo-card:hover {
            transform: translateY(-10px);
            box-shadow: 0 15px 30px rgba(0,0,0,0.12);
        }
        
        .promo-header {
            background-color: var(--primary);
            color: var(--white);
            padding: 20px;
            text-align: center;
        }
        
        .promo-header h3 {
            font-size: 1.5rem;
            margin-bottom: 5px;
        }
        
        .promo-price {
            font-size: 2rem;
            font-weight: 700;
        }
        
        .promo-body {
            padding: 25px;
        }
        
        .promo-body ul {
            list-style-position: inside;
            margin-bottom: 20px;
        }
        
        .promo-body li {
            margin-bottom: 8px;
            color: var(--gray);
        }
        
        /* Scratch Card Coupon Section */
        .scratch-section {
            background-color: var(--primary);
            color: var(--white);
            padding: 60px 0;
            text-align: center;
        }
        
        .scratch-container {
            max-width: 600px;
            margin: 0 auto;
            padding: 20px;
        }
        
        .scratch-card {
            background: linear-gradient(135deg, #f5e8dd 0%, #f9f5f2 100%);
            border-radius: 15px;
            padding: 30px;
            box-shadow: 0 15px 35px rgba(0, 0, 0, 0.2);
            position: relative;
            overflow: hidden;
            margin-bottom: 30px;
        }
        
        .scratch-card h3 {
            color: var(--primary);
            margin-bottom: 20px;
            font-size: 1.8rem;
        }
        
        .scratch-card p {
            color: var(--gray);
            margin-bottom: 25px;
        }
        
        .scratch-area {
            background-color: #cccccc;
            height: 180px;
            border-radius: 10px;
            position: relative;
            margin: 0 auto 20px;
            cursor: pointer;
            overflow: hidden;
            box-shadow: inset 0 0 10px rgba(0,0,0,0.1);
            max-width: 500px;
        }
        
        .scratch-canvas {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            cursor: url('data:image/svg+xml;utf8,<svg xmlns="http://www.w3.org/2000/svg" width="40" height="40" viewBox="0 0 40 40"><circle cx="20" cy="20" r="18" fill="none" stroke="%238a6d5b" stroke-width="4"/></svg>') 20 20, crosshair;
            z-index: 2;
        }
        
        .scratch-pattern {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: repeating-linear-gradient(
                45deg,
                #999,
                #999 10px,
                #888 10px,
                #888 20px
            );
            opacity: 0.7;
            z-index: 1;
        }
        
        .coupon-reveal {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            background-color: var(--white);
            z-index: 0;
            padding: 20px;
            text-align: center;
        }
        
        .coupon-code-revealed {
            background-color: var(--secondary);
            border: 2px dashed var(--accent);
            padding: 15px 30px;
            font-size: 2.2rem;
            font-weight: 700;
            letter-spacing: 3px;
            margin: 15px 0;
            color: var(--primary-dark);
            border-radius: 8px;
            font-family: 'Courier New', monospace;
        }
        
        .discount-amount {
            font-size: 1.5rem;
            color: var(--primary);
            font-weight: 600;
            margin-bottom: 10px;
        }
        
        .scratch-instructions {
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 10px;
            margin-top: 15px;
            color: var(--white);
        }
        
        .scratch-instructions i {
            font-size: 1.2rem;
            animation: bounce 2s infinite;
        }
        
        @keyframes bounce {
            0%, 100% { transform: translateY(0); }
            50% { transform: translateY(-5px); }
        }
        
        .scratch-buttons {
            display: flex;
            gap: 15px;
            justify-content: center;
            margin-top: 20px;
        }
        
        .scratch-btn {
            background-color: var(--accent);
            color: var(--dark);
            border: none;
            padding: 12px 25px;
            border-radius: 4px;
            font-weight: 600;
            font-size: 1rem;
            cursor: pointer;
            transition: var(--transition);
        }
        
        .scratch-btn:hover {
            background-color: var(--white);
            transform: translateY(-3px);
        }
        
        .scratch-btn.secondary {
            background-color: transparent;
            border: 2px solid var(--accent);
            color: var(--accent);
        }
        
        .scratch-btn.secondary:hover {
            background-color: var(--accent);
            color: var(--dark);
        }
        
        .coupon-terms {
            font-size: 0.85rem;
            color: var(--white);
            opacity: 0.8;
            margin-top: 15px;
            max-width: 600px;
            margin-left: auto;
            margin-right: auto;
        }
        
        /* Services Section */
        .services {
            padding: 80px 0;
        }
        
        .service-categories {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(250px, 1fr));
            gap: 30px;
            margin-bottom: 60px;
        }
        
        .category-card {
            background-color: var(--light);
            padding: 30px;
            border-radius: 8px;
            text-align: center;
            transition: var(--transition);
            border-bottom: 4px solid transparent;
        }
        
        .category-card:hover {
            border-bottom-color: var(--primary);
            transform: translateY(-5px);
        }
        
        .category-icon {
            font-size: 2.5rem;
            color: var(--primary);
            margin-bottom: 20px;
        }
        
        .category-card h3 {
            font-size: 1.5rem;
            margin-bottom: 15px;
        }
        
        /* Booking Page */
        .booking-page {
            padding: 100px 0 80px;
            background-color: var(--light);
        }
        
        .booking-container {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 50px;
        }
        
        .booking-form {
            background-color: var(--white);
            padding: 40px;
            border-radius: 8px;
            box-shadow: 0 10px 30px rgba(0,0,0,0.08);
        }
        
        .booking-form h3 {
            margin-bottom: 30px;
            color: var(--primary);
        }
        
        .form-group {
            margin-bottom: 25px;
        }
        
        .form-group label {
            display: block;
            margin-bottom: 8px;
            font-weight: 500;
        }
        
        .form-group input, .form-group select, .form-group textarea {
            width: 100%;
            padding: 12px 15px;
            border: 1px solid #ddd;
            border-radius: 4px;
            font-family: 'Montserrat', sans-serif;
            font-size: 1rem;
            transition: var(--transition);
        }
        
        .form-group input:focus, .form-group select:focus, .form-group textarea:focus {
            outline: none;
            border-color: var(--primary);
            box-shadow: 0 0 0 2px rgba(138, 109, 91, 0.2);
        }
        
        .form-row {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 20px;
        }
        
        .submit-btn {
            background-color: var(--primary);
            color: var(--white);
            border: none;
            padding: 15px 30px;
            border-radius: 4px;
            font-weight: 600;
            font-size: 1.1rem;
            cursor: pointer;
            transition: var(--transition);
            width: 100%;
        }
        
        .submit-btn:hover {
            background-color: var(--primary-dark);
        }
        
        .booking-info {
            padding: 20px;
        }
        
        .booking-info h3 {
            color: var(--primary);
            margin-bottom: 20px;
        }
        
        /* Map Section */
        .map-section {
            padding: 80px 0;
        }
        
        .map-container {
            border-radius: 8px;
            overflow: hidden;
            box-shadow: 0 10px 30px rgba(0,0,0,0.1);
            margin-top: 30px;
        }
        
        .map-wrapper {
            height: 400px;
            width: 100%;
        }
        
        /* Gallery Section */
        .gallery {
            padding: 80px 0;
            background-color: var(--light);
        }
        
        .gallery-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(250px, 1fr));
            gap: 20px;
            margin-top: 40px;
        }
        
        .gallery-item {
            border-radius: 8px;
            overflow: hidden;
            height: 250px;
            position: relative;
        }
        
        .gallery-item img {
            width: 100%;
            height: 100%;
            object-fit: cover;
            transition: var(--transition);
        }
        
        .gallery-item:hover img {
            transform: scale(1.05);
        }
        
        .gallery-caption {
            position: absolute;
            bottom: 0;
            left: 0;
            right: 0;
            background: linear-gradient(transparent, rgba(0,0,0,0.8));
            color: var(--white);
            padding: 15px;
            transform: translateY(10px);
            opacity: 0;
            transition: var(--transition);
        }
        
        .gallery-item:hover .gallery-caption {
            transform: translateY(0);
            opacity: 1;
        }
        
        /* About Section */
        .about {
            padding: 80px 0;
        }
        
        .about-content {
            display: flex;
            align-items: center;
            gap: 50px;
        }
        
        .about-text {
            flex: 1;
        }
        
        .about-image {
            flex: 1;
            border-radius: 8px;
            overflow: hidden;
            box-shadow: 0 10px 30px rgba(0,0,0,0.1);
        }
        
        .about-image img {
            width: 100%;
            height: auto;
            display: block;
        }
        
        .about-text h2 {
            font-size: 2.5rem;
            color: var(--primary);
            margin-bottom: 20px;
        }
        
        .founder-quote {
            background-color: var(--white);
            padding: 25px;
            border-left: 4px solid var(--accent);
            margin-top: 30px;
            font-style: italic;
            box-shadow: 0 5px 15px rgba(0,0,0,0.05);
        }
        
        /* Partners Section */
        .partners {
            padding: 60px 0;
            text-align: center;
        }
        
        .partner-logos {
            display: flex;
            justify-content: center;
            flex-wrap: wrap;
            gap: 40px;
            margin-top: 40px;
        }
        
        .partner-logo {
            height: 60px;
            filter: grayscale(100%);
            opacity: 0.7;
            transition: var(--transition);
        }
        
        .partner-logo:hover {
            filter: grayscale(0);
            opacity: 1;
        }
        
        /* Social Media Section */
        .social-media {
            background-color: var(--primary);
            color: var(--white);
            padding: 60px 0;
            text-align: center;
        }
        
        .social-icons {
            display: flex;
            justify-content: center;
            gap: 30px;
            margin-top: 30px;
            flex-wrap: wrap;
        }
        
        .social-icon {
            display: flex;
            flex-direction: column;
            align-items: center;
            text-decoration: none;
            color: var(--white);
            transition: var(--transition);
            width: 120px;
        }
        
        .social-icon-circle {
            display: flex;
            align-items: center;
            justify-content: center;
            width: 80px;
            height: 80px;
            background-color: rgba(255, 255, 255, 0.1);
            border-radius: 50%;
            font-size: 2.2rem;
            margin-bottom: 10px;
            transition: var(--transition);
        }
        
        .social-icon:hover .social-icon-circle {
            background-color: var(--accent);
            transform: translateY(-5px);
        }
        
        .social-icon:hover {
            color: var(--secondary);
        }
        
        .social-platform {
            font-weight: 600;
            font-size: 0.9rem;
        }
        
        /* WhatsApp Floating Button */
        .whatsapp-float {
            position: fixed;
            bottom: 30px;
            right: 30px;
            background-color: #25D366;
            color: var(--white);
            width: 60px;
            height: 60px;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 2rem;
            box-shadow: 0 4px 15px rgba(37, 211, 102, 0.4);
            z-index: 1000;
            transition: var(--transition);
            text-decoration: none;
        }
        
        .whatsapp-float:hover {
            background-color: #128C7E;
            transform: scale(1.1);
        }
        
        /* Social Media Floating Sidebar */
        .social-sidebar {
            position: fixed;
            left: 20px;
            top: 50%;
            transform: translateY(-50%);
            background-color: var(--white);
            border-radius: 10px;
            box-shadow: 0 5px 20px rgba(0,0,0,0.1);
            padding: 15px 10px;
            z-index: 999;
            display: flex;
            flex-direction: column;
            gap: 15px;
        }
        
        .social-sidebar a {
            color: var(--primary);
            font-size: 1.3rem;
            transition: var(--transition);
            display: flex;
            align-items: center;
            justify-content: center;
            width: 40px;
            height: 40px;
            border-radius: 50%;
        }
        
        .social-sidebar a:hover {
            background-color: var(--secondary);
            transform: translateX(3px);
        }
        
        .social-sidebar .fa-instagram:hover {
            color: #E4405F;
        }
        
        .social-sidebar .fa-facebook-f:hover {
            color: #1877F2;
        }
        
        .social-sidebar .fa-whatsapp:hover {
            color: #25D366;
        }
        
        /* Chat Bot Styles */
        .chat-bot-container {
            position: fixed;
            bottom: 100px;
            right: 30px;
            z-index: 1000;
            font-family: 'Montserrat', sans-serif;
        }
        
        .chat-bot-btn {
            background-color: var(--primary);
            color: white;
            width: 60px;
            height: 60px;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 1.8rem;
            cursor: pointer;
            box-shadow: 0 4px 15px rgba(138, 109, 91, 0.4);
            transition: var(--transition);
            border: none;
        }
        
        .chat-bot-btn:hover {
            background-color: var(--primary-dark);
            transform: scale(1.1);
        }
        
        .chat-bot-window {
            position: absolute;
            bottom: 70px;
            right: 0;
            width: 350px;
            height: 500px;
            background-color: white;
            border-radius: 15px;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.2);
            display: none;
            flex-direction: column;
            overflow: hidden;
        }
        
        .chat-bot-header {
            background-color: var(--primary);
            color: white;
            padding: 15px 20px;
            display: flex;
            align-items: center;
            justify-content: space-between;
        }
        
        .chat-bot-header h3 {
            font-size: 1.2rem;
            margin: 0;
            color: white;
        }
        
        .close-chat {
            background: none;
            border: none;
            color: white;
            font-size: 1.5rem;
            cursor: pointer;
        }
        
        .chat-messages {
            flex: 1;
            padding: 20px;
            overflow-y: auto;
            display: flex;
            flex-direction: column;
            gap: 15px;
        }
        
        .message {
            max-width: 80%;
            padding: 12px 15px;
            border-radius: 18px;
            line-height: 1.4;
        }
        
        .bot-message {
            background-color: var(--light);
            align-self: flex-start;
            border-bottom-left-radius: 5px;
        }
        
        .user-message {
            background-color: var(--primary);
            color: white;
            align-self: flex-end;
            border-bottom-right-radius: 5px;
        }
        
        .chat-input {
            padding: 15px;
            border-top: 1px solid #eee;
            display: flex;
            gap: 10px;
        }
        
        .chat-input input {
            flex: 1;
            padding: 12px 15px;
            border: 1px solid #ddd;
            border-radius: 25px;
            font-family: 'Montserrat', sans-serif;
        }
        
        .chat-input button {
            background-color: var(--primary);
            color: white;
            border: none;
            width: 45px;
            height: 45px;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            cursor: pointer;
            transition: var(--transition);
        }
        
        .chat-input button:hover {
            background-color: var(--primary-dark);
        }
        
        .quick-options {
            display: flex;
            flex-wrap: wrap;
            gap: 8px;
            margin-top: 10px;
        }
        
        .quick-option {
            background-color: var(--secondary);
            border: none;
            padding: 8px 12px;
            border-radius: 20px;
            font-size: 0.85rem;
            cursor: pointer;
            transition: var(--transition);
        }
        
        .quick-option:hover {
            background-color: var(--accent);
        }
        
        .chat-bot-window.active {
            display: flex;
        }
        
        /* Footer */
        footer {
            background-color: var(--dark);
            color: var(--white);
            padding: 60px 0 30px;
        }
        
        .footer-content {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 40px;
            margin-bottom: 40px;
        }
        
        .footer-column h3 {
            color: var(--accent);
            font-size: 1.3rem;
            margin-bottom: 25px;
            position: relative;
            padding-bottom: 10px;
        }
        
        .footer-column h3::after {
            content: '';
            position: absolute;
            bottom: 0;
            left: 0;
            width: 40px;
            height: 2px;
            background-color: var(--primary);
        }
        
        .footer-column ul {
            list-style: none;
        }
        
        .footer-column ul li {
            margin-bottom: 12px;
        }
        
        .footer-column a {
            color: #cccccc;
            text-decoration: none;
            transition: var(--transition);
        }
        
        .footer-column a:hover {
            color: var(--accent);
            padding-left: 5px;
        }
        
        .contact-info {
            display: flex;
            align-items: flex-start;
            gap: 10px;
            margin-bottom: 15px;
        }
        
        .copyright {
            text-align: center;
            padding-top: 30px;
            border-top: 1px solid rgba(255,255,255,0.1);
            color: #aaaaaa;
            font-size: 0.9rem;
        }
        
        .booking-note {
            background-color: var(--secondary);
            padding: 15px;
            border-radius: 4px;
            margin-top: 20px;
            font-size: 0.9rem;
            color: var(--dark);
        }
        
        .footer-social {
            display: flex;
            gap: 15px;
            margin-top: 20px;
        }
        
        .footer-social a {
            display: inline-flex;
            align-items: center;
            justify-content: center;
            width: 40px;
            height: 40px;
            background-color: rgba(255,255,255,0.1);
            border-radius: 50%;
            color: var(--white);
            font-size: 1.2rem;
            transition: var(--transition);
        }
        
        .footer-social a:hover {
            background-color: var(--accent);
            transform: translateY(-3px);
        }
        
        /* Responsive Design */
        @media (max-width: 992px) {
            .about-content, .booking-container {
                flex-direction: column;
                grid-template-columns: 1fr;
            }
            
            .hero-slider {
                height: 70vh;
                min-height: 500px;
            }
            
            .slider-content h2 {
                font-size: 2.8rem;
            }
            
            .form-row {
                grid-template-columns: 1fr;
            }
            
            .social-sidebar {
                display: none;
            }
            
            .chat-bot-window {
                width: 300px;
                height: 450px;
            }
            
            .scratch-area {
                height: 150px;
            }
            
            .coupon-code-revealed {
                font-size: 1.8rem;
                padding: 12px 20px;
            }
        }
        
        @media (max-width: 768px) {
            nav {
                display: none;
                position: absolute;
                top: 100%;
                left: 0;
                width: 100%;
                background-color: var(--white);
                box-shadow: 0 10px 20px rgba(0,0,0,0.1);
                padding: 20px;
            }
            
            nav.active {
                display: block;
            }
            
            nav ul {
                flex-direction: column;
                gap: 15px;
            }
            
            .mobile-menu-btn {
                display: block;
            }
            
            .phone-cta span {
                display: none;
            }
            
            .social-header {
                display: none;
            }
            
            .hero-slider {
                padding-top: 140px;
                height: 60vh;
                min-height: 400px;
            }
            
            .slider-content h2 {
                font-size: 2.2rem;
            }
            
            .slider-content p {
                font-size: 1.1rem;
            }
            
            .section-title h2 {
                font-size: 2rem;
            }
            
            .social-icons {
                gap: 15px;
            }
            
            .social-icon {
                width: 100px;
            }
            
            .chat-bot-container {
                bottom: 90px;
                right: 20px;
            }
            
            .chat-bot-window {
                width: 280px;
                right: -10px;
            }
            
            .scratch-card {
                padding: 20px;
            }
            
            .coupon-code-revealed {
                font-size: 1.6rem;
            }
            
            .slider-nav {
                bottom: 20px;
            }
            
            .slider-autoplay {
                right: 20px;
                bottom: 20px;
            }
        }
        
        @media (max-width: 576px) {
            .hero-slider {
                height: 50vh;
                min-height: 350px;
            }
            
            .slider-content h2 {
                font-size: 1.8rem;
            }
            
            .slider-content p {
                font-size: 1rem;
            }
            
            .cta-button {
                display: block;
                margin: 10px 0;
            }
            
            .promo-card {
                min-width: 280px;
            }
            
            .social-icon {
                width: 80px;
            }
            
            .social-icon-circle {
                width: 60px;
                height: 60px;
                font-size: 1.8rem;
            }
            
            .chat-bot-container {
                bottom: 80px;
                right: 15px;
            }
            
            .chat-bot-window {
                width: 260px;
                height: 400px;
            }
            
            .scratch-buttons {
                flex-direction: column;
            }
            
            .scratch-btn {
                width: 100%;
            }
            
            .coupon-code-revealed {
                font-size: 1.4rem;
                padding: 10px 15px;
            }
            
            .slider-arrows {
                display: none;
            }
        }
        
        /* Modal for Coupon Details */
        .modal {
            display: none;
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background-color: rgba(0,0,0,0.7);
            z-index: 2000;
            align-items: center;
            justify-content: center;
        }
        
        .modal-content {
            background-color: var(--white);
            padding: 40px;
            border-radius: 8px;
            max-width: 500px;
            width: 90%;
            text-align: center;
            position: relative;
        }
        
        .close-modal {
            position: absolute;
            top: 15px;
            right: 20px;
            font-size: 1.8rem;
            cursor: pointer;
            color: var(--gray);
        }
        
        .modal h3 {
            color: var(--primary);
            margin-bottom: 20px;
        }
        
        /* Page visibility control */
        .page {
            display: none;
        }
        
        .page.active {
            display: block;
        }
    </style>
</head>
<body>
    <!-- Social Media Floating Sidebar -->
    <div class="social-sidebar">
        <a href="https://www.instagram.com/studiossalaonart?igsh=NnQwcm5meWExZWhy&utm_source=qr" target="_blank" title="Follow us on Instagram">
            <i class="fab fa-instagram"></i>
        </a>
        <a href="https://www.facebook.com/profile.php?id=61584290236085" target="_blank" title="Like us on Facebook">
            <i class="fab fa-facebook-f"></i>
        </a>
        <a href="https://wa.me/919217045333" target="_blank" title="Message us on WhatsApp">
            <i class="fab fa-whatsapp"></i>
        </a>
    </div>

    <!-- Chat Bot -->
    <div class="chat-bot-container">
        <button class="chat-bot-btn" id="chatBotBtn">
            <i class="fas fa-comment-dots"></i>
        </button>
        
        <div class="chat-bot-window" id="chatBotWindow">
            <div class="chat-bot-header">
                <h3><i class="fas fa-robot"></i> Salon Assistant</h3>
                <button class="close-chat" id="closeChatBtn">&times;</button>
            </div>
            
            <div class="chat-messages" id="chatMessages">
                <div class="message bot-message">
                    Hello! I'm your Salon Assistant. How can I help you today?
                </div>
                <div class="message bot-message">
                    You can ask me about:
                    <div class="quick-options">
                        <button class="quick-option" data-question="services">Our Services</button>
                        <button class="quick-option" data-question="pricing">Pricing</button>
                        <button class="quick-option" data-question="booking">Book Appointment</button>
                        <button class="quick-option" data-question="hours">Opening Hours</button>
                        <button class="quick-option" data-question="coupon">Special Offers</button>
                        <button class="quick-option" data-question="location">Location</button>
                    </div>
                </div>
            </div>
            
            <div class="chat-input">
                <input type="text" id="chatInput" placeholder="Type your message...">
                <button id="sendMessageBtn">
                    <i class="fas fa-paper-plane"></i>
                </button>
            </div>
        </div>
    </div>

    <!-- WhatsApp Floating Button -->
    <a href="https://wa.me/919217045333?text=Hi,%20I'd%20like%20to%20book%20an%20appointment%20at%20Studio's%20Salon%20Art" class="whatsapp-float" target="_blank" title="Chat with us on WhatsApp">
        <i class="fab fa-whatsapp"></i>
    </a>

    <!-- Header & Navigation -->
    <header>
        <div class="container header-container">
            <div class="logo">
                <i class="fas fa-spa"></i>
                <h1>Studio's <span>Salon Art</span></h1>
            </div>
            
            <div class="social-header">
                <a href="https://www.instagram.com/studiossalaonart?igsh=NnQwcm5meWExZWhy&utm_source=qr" target="_blank" title="Instagram">
                    <i class="fab fa-instagram"></i>
                </a>
                <a href="https://www.facebook.com/profile.php?id=61584290236085" target="_blank" title="Facebook">
                    <i class="fab fa-facebook-f"></i>
                </a>
            </div>
            
            <button class="mobile-menu-btn" id="mobileMenuBtn">
                <i class="fas fa-bars"></i>
            </button>
            
            <nav id="mainNav">
                <ul>
                    <li><a href="#home" class="nav-link active" data-page="home">Home</a></li>
                    <li><a href="#about" class="nav-link" data-page="about">About</a></li>
                    <li><a href="#services" class="nav-link" data-page="services">Services</a></li>
                    <li><a href="#booking" class="nav-link" data-page="booking">Book Now</a></li>
                    <li><a href="#gallery" class="nav-link" data-page="gallery">Gallery</a></li>
                    <li><a href="#contact" class="nav-link" data-page="contact">Contact</a></li>
                </ul>
            </nav>
            
            <a href="#booking" class="phone-cta nav-link" data-page="booking">
                <i class="fas fa-calendar-alt"></i>
                <span>Book Now</span>
            </a>
        </div>
    </header>

    <!-- Home Page -->
    <section class="page active" id="home-page">
        <!-- Hero Slider Section -->
        <section class="hero-slider" id="home">
            <div class="slider-container">
                <!-- Slide 1: Hair Styling -->
                <div class="slider-slide active">
                    <img src="https://images.unsplash.com/photo-1562322140-8baeececf3df?ixlib=rb-4.0.3&ixid=M3wxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8fA%3D%3D&auto=format&fit=crop&w=1600&q=80" alt="Professional Hair Styling" class="slider-image">
                    <div class="slider-overlay"></div>
                    <div class="slider-content">
                        <h2>Expert Hair Transformations</h2>
                        <p>Experience precision cutting, coloring, and styling by our master artists. Transform your look with our premium hair services.</p>
                        <a href="#services" class="cta-button nav-link" data-page="services">View Hair Services</a>
                        <a href="#booking" class="cta-button cta-secondary nav-link" data-page="booking">Book Now</a>
                    </div>
                </div>
                
                <!-- Slide 2: Facial & Skin Care -->
                <div class="slider-slide">
                    <img src="https://images.unsplash.com/photo-1540555700478-4be289fbecef?ixlib=rb-4.0.3&ixid=M3wxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8fA%3D%3D&auto=format&fit=crop&w=1600&q=80" alt="Luxury Facial Treatment" class="slider-image">
                    <div class="slider-overlay"></div>
                    <div class="slider-content">
                        <h2>Premium Skin Rejuvenation</h2>
                        <p>Indulge in our O3+, Casmara, and Hydra facials for radiant, glowing skin. Professional treatments for total rejuvenation.</p>
                        <a href="#services" class="cta-button nav-link" data-page="services">View Skin Treatments</a>
                        <a href="#booking" class="cta-button cta-secondary nav-link" data-page="booking">Book Now</a>
                    </div>
                </div>
                
                <!-- Slide 3: Men's Grooming -->
                <div class="slider-slide">
                    <img src="https://images.unsplash.com/photo-1599351431202-1e0f0137899a?ixlib=rb-4.0.3&ixid=M3wxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8fA%3D%3D&auto=format&fit=crop&w=1600&q=80" alt="Men's Grooming Service" class="slider-image">
                    <div class="slider-overlay"></div>
                    <div class="slider-content">
                        <h2>Gentleman's Grooming</h2>
                        <p>Sharp looks for the modern man. Professional haircuts, beard styling, and relaxation treatments in a premium environment.</p>
                        <a href="#services" class="cta-button nav-link" data-page="services">View Gents Services</a>
                        <a href="#booking" class="cta-button cta-secondary nav-link" data-page="booking">Book Now</a>
                    </div>
                </div>
                
                <!-- Slide 4: Keratin Treatment -->
                <div class="slider-slide">
                    <img src="https://images.unsplash.com/photo-1605497788044-5a32c7078486?ixlib=rb-4.0.3&ixid=M3wxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8fA%3D%3D&auto=format&fit=crop&w=1600&q=80" alt="Keratin Hair Treatment" class="slider-image">
                    <div class="slider-overlay"></div>
                    <div class="slider-content">
                        <h2>Keratin & Smoothing Treatments</h2>
                        <p>Get silky, manageable hair with our premium keratin treatments. Any length, professional application, guaranteed results.</p>
                        <a href="#promotions" class="cta-button" onclick="showPage('home')">View Keratin Special</a>
                        <a href="#booking" class="cta-button cta-secondary nav-link" data-page="booking">Book Treatment</a>
                    </div>
                </div>
                
                <!-- Slide 5: Video - Salon Atmosphere -->
                <div class="slider-slide">
                    <video autoplay muted loop class="slider-video">
                        <source src="https://assets.mixkit.co/videos/preview/mixkit-woman-getting-a-haircut-at-the-hairdresser-3982-large.mp4" type="video/mp4">
                        Your browser does not support the video tag.
                    </video>
                    <div class="slider-overlay"></div>
                    <div class="slider-content">
                        <h2>Luxury Salon Experience</h2>
                        <p>Step into our sanctuary of style and relaxation. Where artistry meets comfort for your complete transformation.</p>
                        <a href="#about" class="cta-button nav-link" data-page="about">Our Story</a>
                        <a href="#gallery" class="cta-button cta-secondary nav-link" data-page="gallery">View Gallery</a>
                    </div>
                </div>
                
                <!-- Slider Navigation -->
                <div class="slider-nav">
                    <div class="slider-arrows">
                        <button class="slider-arrow prev-slide">
                            <i class="fas fa-chevron-left"></i>
                        </button>
                        <button class="slider-arrow next-slide">
                            <i class="fas fa-chevron-right"></i>
                        </button>
                    </div>
                    
                    <div class="slider-dots">
                        <button class="slider-dot active" data-slide="0"></button>
                        <button class="slider-dot" data-slide="1"></button>
                        <button class="slider-dot" data-slide="2"></button>
                        <button class="slider-dot" data-slide="3"></button>
                        <button class="slider-dot" data-slide="4"></button>
                    </div>
                    
                    <div class="slider-autoplay">
                        <button class="autoplay-btn" id="autoplayBtn">
                            <i class="fas fa-pause"></i>
                        </button>
                    </div>
                </div>
            </div>
        </section>

        <!-- Scratch Card Coupon Section -->
        <section class="scratch-section">
            <div class="container scratch-container">
                <div class="scratch-card">
                    <h3>Scratch & Win Special Offer!</h3>
                    <p>Scratch the card below to reveal your exclusive discount code. Each visitor gets a unique code!</p>
                    
                    <div class="scratch-area" id="scratchArea">
                        <canvas class="scratch-canvas" id="scratchCanvas"></canvas>
                        <div class="scratch-pattern"></div>
                        <div class="coupon-reveal" id="couponReveal">
                            <div class="discount-amount">Your Exclusive Discount:</div>
                            <div class="coupon-code-revealed" id="couponCodeDisplay">STUDIO15</div>
                            <p>Use this code when booking to claim your discount!</p>
                        </div>
                    </div>
                    
                    <div class="scratch-instructions">
                        <i class="fas fa-mouse-pointer"></i>
                        <span>Drag your mouse or finger to scratch the card</span>
                    </div>
                    
                    <div class="scratch-buttons">
                        <button class="scratch-btn" id="revealAllBtn">Reveal All</button>
                        <button class="scratch-btn secondary" id="resetScratchBtn">Reset Card</button>
                        <button class="scratch-btn secondary" id="howToUseBtn">How to Use</button>
                    </div>
                </div>
                
                <div class="coupon-terms">
                    <p>*Each visitor gets a unique coupon code. Code must be used within 30 days of generation. Valid for first-time clients only. Cannot be combined with other offers.</p>
                </div>
            </div>
        </section>

        <!-- Promotions Section -->
        <section class="promotions" id="promotions">
            <div class="container">
                <div class="section-title">
                    <h2>Current Promotions</h2>
                    <p>Spoil Yourself, You Deserve It!</p>
                </div>
                
                <div class="promo-slider">
                    <!-- Keratin Special -->
                    <div class="promo-card">
                        <div class="promo-header">
                            <h3>Keratin Special</h3>
                            <div class="promo-price">₹4999</div>
                        </div>
                        <div class="promo-body">
                            <ul>
                                <li>Keratin Treatment (Any Length)</li>
                                <li>Free Haircut Included</li>
                                <li>Professional Styling</li>
                                <li>Premium Products</li>
                            </ul>
                            <a href="#booking" class="cta-button nav-link" data-page="booking" style="padding: 10px 20px; font-size: 1rem; width: 100%; text-align: center;">Book Now</a>
                        </div>
                    </div>
                    
                    <!-- Pick Your Perfect Bundle -->
                    <div class="promo-card">
                        <div class="promo-header">
                            <h3>Classic Trio</h3>
                            <div class="promo-price">₹999</div>
                        </div>
                        <div class="promo-body">
                            <p><strong>Pick Your Perfect Bundle</strong></p>
                            <ul>
                                <li>Choose any 3 basic services</li>
                                <li>Waxing, Pedicure, Trim</li>
                                <li>Customizable to your needs</li>
                                <li>Limited time offer</li>
                            </ul>
                            <a href="#booking" class="cta-button nav-link" data-page="booking" style="padding: 10px 20px; font-size: 1rem; width: 100%; text-align: center;">Customize Now</a>
                        </div>
                    </div>
                    
                    <!-- Global Hair Color -->
                    <div class="promo-card">
                        <div class="promo-header">
                            <h3>Global Hair Color</h3>
                            <div class="promo-price">₹2999</div>
                        </div>
                        <div class="promo-body">
                            <ul>
                                <li>Full Head Color Application</li>
                                <li>Professional Consultation</li>
                                <li>Premium Color Products</li>
                                <li>Conditioning Treatment</li>
                            </ul>
                            <a href="#booking" class="cta-button nav-link" data-page="booking" style="padding: 10px 20px; font-size: 1rem; width: 100%; text-align: center;">Book Now</a>
                        </div>
                    </div>
                    
                    <!-- Ultimate Indulgence -->
                    <div class="promo-card">
                        <div class="promo-header">
                            <h3>Ultimate Indulgence</h3>
                            <div class="promo-price">₹4199</div>
                        </div>
                        <div class="promo-body">
                            <ul>
                                <li>Full Body Bleach</li>
                                <li>O3+ Facial</li>
                                <li>Luxury Manicure</li>
                                <li>Pedicure & More</li>
                            </ul>
                            <a href="#booking" class="cta-button nav-link" data-page="booking" style="padding: 10px 20px; font-size: 1rem; width: 100%; text-align: center;">Pamper Yourself</a>
                        </div>
                    </div>
                </div>
                
                <div class="booking-note">
                    <p><i class="fas fa-info-circle"></i> <strong>Booking Policy:</strong> Appointments recommended. Prices subject to change. '*' denotes starting price.</p>
                </div>
            </div>
        </section>

        <!-- Services Preview Section -->
        <section class="services" id="services-preview">
            <div class="container">
                <div class="section-title">
                    <h2>Our Services</h2>
                    <p>Expert care for your crowning glory and skin rejuvenation</p>
                </div>
                
                <div class="service-categories">
                    <div class="category-card">
                        <div class="category-icon">
                            <i class="fas fa-cut"></i>
                        </div>
                        <h3>Hair Transformations</h3>
                        <p>Balayage, Ombré, Keratin, Botox, Nano Plastia, Precision Cutting</p>
                        <a href="#services" class="nav-link" data-page="services" style="margin-top: 15px; display: inline-block; color: var(--primary); font-weight: 600;">View Details →</a>
                    </div>
                    
                    <div class="category-card">
                        <div class="category-icon">
                            <i class="fas fa-spa"></i>
                        </div>
                        <h3>Skin Aesthetics</h3>
                        <p>O3+, Casmara, Hydra Facials, Waxing, D-Tan, Professional Treatments</p>
                        <a href="#services" class="nav-link" data-page="services" style="margin-top: 15px; display: inline-block; color: var(--primary); font-weight: 600;">View Details →</a>
                    </div>
                    
                    <div class="category-card">
                        <div class="category-icon">
                            <i class="fas fa-user-tie"></i>
                        </div>
                        <h3>Gentleman's Grooming</h3>
                        <p>Barbering, Beard Styling, Relaxation Treatments, Modern Looks</p>
                        <a href="#services" class="nav-link" data-page="services" style="margin-top: 15px; display: inline-block; color: var(--primary); font-weight: 600;">View Details →</a>
                    </div>
                    
                    <div class="category-card">
                        <div class="category-icon">
                            <i class="fas fa-gem"></i>
                        </div>
                        <h3>Exclusive Packages</h3>
                        <p>Customizable "Pick Your Perfect" bundles for total rejuvenation</p>
                        <a href="#services" class="nav-link" data-page="services" style="margin-top: 15px; display: inline-block; color: var(--primary); font-weight: 600;">View Details →</a>
                    </div>
                </div>
            </div>
        </section>
    </section>

    <!-- About Page -->
    <section class="page" id="about-page">
        <section class="about" style="padding-top: 100px;">
            <div class="container">
                <div class="section-title">
                    <h2>About Us</h2>
                    <p>Revealing the Beauty Within</p>
                </div>
                
                <div class="about-content">
                    <div class="about-text">
                        <h2>Meet Our Master Artist</h2>
                        <p><strong>Shoaib | Founder & Creative Director</strong></p>
                        <p>The creative pulse of Studio's Salon Art, Shoaib brings 12+ years of industry excellence to New Friends Colony. Specializing in advanced hair transformations (color correction, precision cutting) and bespoke styling, he views every client as a canvas.</p>
                        <p>Under his direction, the salon has become a sanctuary where experience meets innovation.</p>
                        
                        <div class="founder-quote">
                            <p>"Welcome to Studio's Salon Art. I founded this space with one goal in mind: to create a place where artistry and comfort coexist. With over 12 years of experience behind the chair, I've learned that a great haircut or treatment isn't just about the technique—it's about how it makes you feel."</p>
                            <p style="margin-top: 15px; font-weight: 600;">— Shoaib, Chief Artist & Owner</p>
                        </div>
                    </div>
                    
                    <div class="about-image">
                        <img src="https://images.unsplash.com/photo-1599351431202-1e0f0137899a?ixlib=rb-4.0.3&ixid=M3wxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8fA%3D%3D&auto=format&fit=crop&w=800&q=80" alt="Shoaib, Master Stylist at Studio's Salon Art">
                    </div>
                </div>
            </div>
        </section>

        <!-- Partners Section -->
        <section class="partners">
            <div class="container">
                <div class="section-title">
                    <h2>Our Premium Partners</h2>
                    <p>We use only the highest quality products</p>
                </div>
                
                <div class="partner-logos">
                    <img src="https://upload.wikimedia.org/wikipedia/commons/thumb/8/8a/Loreal_logo.svg/2560px-Loreal_logo.svg.png" alt="L'Oréal" class="partner-logo" style="height: 40px;">
                    <img src="https://www.o3plus.com/pub/media/logo/stores/1/logo.png" alt="O3+" class="partner-logo">
                    <img src="https://www.casmaracostmetics.com/wp-content/uploads/2020/07/logo-casmara-cosmetics.png" alt="Casmara" class="partner-logo">
                    <img src="https://www.biotop.it/wp-content/uploads/2019/05/logo-BIOTOP-bianco-1.png" alt="Biotop" class="partner-logo">
                </div>
            </div>
        </section>
    </section>

    <!-- Services Page -->
    <section class="page" id="services-page">
        <section class="services" style="padding-top: 100px;">
            <div class="container">
                <div class="section-title">
                    <h2>Our Complete Services</h2>
                    <p>Expert care for men, women, and children</p>
                </div>
                
                <div class="service-categories">
                    <div class="category-card">
                        <div class="category-icon">
                            <i class="fas fa-female"></i>
                        </div>
                        <h3>Ladies Menu</h3>
                        <p>Haircuts, Styling, Coloring, Treatments, Bridal Packages, Waxing, Facials, Manicure, Pedicure</p>
                        <p style="margin-top: 15px; color: var(--primary); font-weight: 600;">Starting from ₹499*</p>
                    </div>
                    
                    <div class="category-card">
                        <div class="category-icon">
                            <i class="fas fa-male"></i>
                        </div>
                        <h3>Gents Menu</h3>
                        <p>Haircuts, Shaves, Beard Styling, Hair Treatments, Facials, Cleanups, Waxing, Pedicure</p>
                        <p style="margin-top: 15px; color: var(--primary); font-weight: 600;">Starting from ₹299*</p>
                    </div>
                    
                    <div class="category-card">
                        <div class="category-icon">
                            <i class="fas fa-palette"></i>
                        </div>
                        <h3>Chemical & Color Work</h3>
                        <p>Keratin, Smoothing, Rebonding, Global Color, Highlights, Balayage, Ombré, Color Correction</p>
                        <p style="margin-top: 15px; color: var(--primary); font-weight: 600;">Starting from ₹2999*</p>
                    </div>
                    
                    <div class="category-card">
                        <div class="category-icon">
                            <i class="fas fa-crown"></i>
                        </div>
                        <h3>Exclusive Packages</h3>
                        <p>Classic Trio ₹999, Premium Quartet ₹1599, Luxury Quintet ₹2199, Ultimate Indulgence ₹4199</p>
                        <p style="margin-top: 15px; color: var(--primary); font-weight: 600;">Customizable Packages</p>
                    </div>
                </div>
                
                <div class="booking-note" style="margin-top: 40px;">
                    <p><i class="fas fa-info-circle"></i> <strong>Note:</strong> All prices are subject to change. '*' denotes starting price. For exact pricing, please consult with our stylists during your appointment.</p>
                </div>
            </div>
        </section>
    </section>

    <!-- Booking Page -->
    <section class="page" id="booking-page">
        <section class="booking-page" style="padding-top: 100px;">
            <div class="container">
                <div class="section-title">
                    <h2>Book Your Appointment</h2>
                    <p>Schedule your visit to experience professional styling</p>
                </div>
                
                <div class="booking-container">
                    <div class="booking-form">
                        <h3>Appointment Request</h3>
                        <form id="bookingForm">
                            <div class="form-row">
                                <div class="form-group">
                                    <label for="name">Full Name *</label>
                                    <input type="text" id="name" name="name" required>
                                </div>
                                <div class="form-group">
                                    <label for="phone">Phone Number *</label>
                                    <input type="tel" id="phone" name="phone" required>
                                </div>
                            </div>
                            
                            <div class="form-group">
                                <label for="email">Email Address</label>
                                <input type="email" id="email" name="email">
                            </div>
                            
                            <div class="form-row">
                                <div class="form-group">
                                    <label for="date">Preferred Date *</label>
                                    <input type="date" id="date" name="date" required>
                                </div>
                                <div class="form-group">
                                    <label for="time">Preferred Time *</label>
                                    <select id="time" name="time" required>
                                        <option value="">Select Time</option>
                                        <option value="10:00">10:00 AM</option>
                                        <option value="11:00">11:00 AM</option>
                                        <option value="12:00">12:00 PM</option>
                                        <option value="13:00">1:00 PM</option>
                                        <option value="14:00">2:00 PM</option>
                                        <option value="15:00">3:00 PM</option>
                                        <option value="16:00">4:00 PM</option>
                                        <option value="17:00">5:00 PM</option>
                                        <option value="18:00">6:00 PM</option>
                                    </select>
                                </div>
                            </div>
                            
                            <div class="form-group">
                                <label for="service">Service Category *</label>
                                <select id="service" name="service" required>
                                    <option value="">Select Service</option>
                                    <option value="hair">Hair Services</option>
                                    <option value="skin">Skin Treatments</option>
                                    <option value="gents">Gentleman's Grooming</option>
                                    <option value="package">Package Deal</option>
                                    <option value="color">Color/Chemical Work</option>
                                    <option value="other">Other</option>
                                </select>
                            </div>
                            
                            <div class="form-group">
                                <label for="coupon">Coupon Code (if any)</label>
                                <input type="text" id="coupon" name="coupon" placeholder="e.g., STUDIO15" value="STUDIO15">
                            </div>
                            
                            <div class="form-group">
                                <label for="message">Additional Notes</label>
                                <textarea id="message" name="message" rows="4" placeholder="Any specific requirements or questions..."></textarea>
                            </div>
                            
                            <button type="submit" class="submit-btn">Book Appointment</button>
                        </form>
                    </div>
                    
                    <div class="booking-info">
                        <h3>Booking Information</h3>
                        <div class="contact-info">
                            <i class="fas fa-clock"></i>
                            <div>
                                <p><strong>Operating Hours:</strong></p>
                                <p>Monday - Saturday: 10:00 AM - 8:00 PM</p>
                                <p>Sunday: 11:00 AM - 6:00 PM</p>
                            </div>
                        </div>
                        
                        <div class="contact-info">
                            <i class="fas fa-map-marker-alt"></i>
                            <div>
                                <p><strong>Location:</strong></p>
                                <p>GROUND FLOOR, 134/A, Navjivan Buddh Vihar</p>
                                <p>Taimoor Nagar, New Friends Colony</p>
                                <p>New Delhi, Delhi 110065</p>
                            </div>
                        </div>
                        
                        <div class="contact-info">
                            <i class="fas fa-phone-alt"></i>
                            <div>
                                <p><strong>Contact:</strong></p>
                                <p>Phone: 9217045333</p>
                                <p>WhatsApp: +91 92170 45333</p>
                            </div>
                        </div>
                        
                        <div class="booking-note">
                            <p><strong>Important Notes:</strong></p>
                            <ul style="padding-left: 20px; margin-top: 10px;">
                                <li>Appointments are recommended to avoid waiting</li>
                                <li>Please arrive 5-10 minutes before your appointment</li>
                                <li>Cancellations should be made at least 4 hours in advance</li>
                                <li>Prices are subject to change</li>
                                <li>Coupon codes must be mentioned at time of booking</li>
                            </ul>
                        </div>
                    </div>
                </div>
            </div>
        </section>
    </section>

    <!-- Gallery Page -->
    <section class="page" id="gallery-page">
        <section class="gallery" style="padding-top: 100px;">
            <div class="container">
                <div class="section-title">
                    <h2>Our Gallery</h2>
                    <p>See the transformations from our talented artists</p>
                </div>
                
                <div class="gallery-grid">
                    <div class="gallery-item">
                        <img src="https://images.unsplash.com/photo-1596462502278-27bfdc403348?ixlib=rb-4.0.3&ixid=M3wxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8fA%3D%3D&auto=format&fit=crop&w=800&q=80" alt="Hair Coloring Service">
                        <div class="gallery-caption">
                            <p>Hair Coloring & Styling</p>
                        </div>
                    </div>
                    
                    <div class="gallery-item">
                        <img src="https://images.unsplash.com/photo-1560851248-7a2a48efb433?ixlib=rb-4.0.3&ixid=M3wxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8fA%3D%3D&auto=format&fit=crop&w-800&q=80" alt="Bridal Makeup">
                        <div class="gallery-caption">
                            <p>Bridal Makeup & Hair</p>
                        </div>
                    </div>
                    
                    <div class="gallery-item">
                        <img src="https://images.unsplash.com/photo-1580618672591-eb180b1a973f?ixlib=rb-4.0.3&ixid=M3wxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8fA%3D%3D&auto=format&fit=crop&w=800&q=80" alt="Facial Treatment">
                        <div class="gallery-caption">
                            <p>Premium Facial Treatment</p>
                        </div>
                    </div>
                    
                    <div class="gallery-item">
                        <img src="https://images.unsplash.com/photo-1598706645705-5cf5c97f6bc5?ixlib=rb-4.0.3&ixid=M3wxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8fA%3D%3D&auto=format&fit=crop&w=800&q=80" alt="Gentleman's Grooming">
                        <div class="gallery-caption">
                            <p>Gentleman's Grooming</p>
                        </div>
                    </div>
                    
                    <div class="gallery-item">
                        <img src="https://images.unsplash.com/photo-1605497788044-5a32c7078486?ixlib=rb-4.0.3&ixid=M3wxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8fA%3D%3D&auto=format&fit=crop&w=800&q=80" alt="Keratin Treatment">
                        <div class="gallery-caption">
                            <p>Keratin Treatment Results</p>
                        </div>
                    </div>
                    
                    <div class="gallery-item">
                        <img src="https://images.unsplash.com/photo-1616394584738-fc6e612e71b9?ixlib=rb-4.0.3&ixid=M3wxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8fA%3D%3D&auto=format&fit=crop&w=800&q=80" alt="Kids Haircut">
                        <div class="gallery-caption">
                            <p>Kids Haircut & Styling</p>
                        </div>
                    </div>
                </div>
            </div>
        </section>
    </section>

    <!-- Contact Page -->
    <section class="page" id="contact-page">
        <!-- Map Section -->
        <section class="map-section" style="padding-top: 100px;">
            <div class="container">
                <div class="section-title">
                    <h2>Find Us</h2>
                    <p>Visit our salon in New Friends Colony</p>
                </div>
                
                <div class="map-container">
                    <div class="map-wrapper">
                        <!-- Updated Google Map with exact coordinates -->
                        <iframe src="https://www.google.com/maps/embed?pb=!1m18!1m12!1m3!1d3504.7160159872995!2d77.23426677538488!3d28.55374107571088!2m3!1f0!2f0!3f0!3m2!1i1024!2i768!4f13.1!3m3!1m2!1s0x390ce3e6d2e34c6f%3A0x7e12eb6c11e4ff22!2sStudio%27s%20Salon%20Art%2C%20134%2FA%2C%20Ground%20Floor%2C%20Navjivan%20Buddh%20Vihar%2C%20Taimoor%20Nagar%2C%20New%20Friends%20Colony%2C%20New%20Delhi%2C%20Delhi%20110065!5e0!3m2!1sen!2sin!4v1712321234567!5m2!1sen!2sin" 
                                width="100%" 
                                height="400" 
                                style="border:0;" 
                                allowfullscreen="" 
                                loading="lazy" 
                                referrerpolicy="no-referrer-when-downgrade"
                                title="Studio's Salon Art Location">
                        </iframe>
                    </div>
                </div>
                
                <div class="contact-info" style="margin-top: 30px; justify-content: center; text-align: center;">
                    <div>
                        <p><strong>Studio's Salon Art</strong></p>
                        <p>GROUND FLOOR, 134/A, Navjivan Buddh Vihar</p>
                        <p>Taimoor Nagar, New Friends Colony</p>
                        <p>New Delhi, Delhi 110065</p>
                        <p>Phone: 9217045333 | WhatsApp: +91 92170 45333</p>
                    </div>
                </div>
            </div>
        </section>

        <!-- Social Media Section -->
        <section class="social-media">
            <div class="container">
                <div class="section-title">
                    <h2 style="color: var(--white);">Connect With Us</h2>
                    <p style="color: var(--secondary);">Follow us for updates, tips, and exclusive offers</p>
                </div>
                
                <div class="social-icons">
                    <a href="https://www.instagram.com/studiossalaonart?igsh=NnQwcm5meWExZWhy&utm_source=qr" class="social-icon" target="_blank">
                        <div class="social-icon-circle">
                            <i class="fab fa-instagram"></i>
                        </div>
                        <span class="social-platform">Instagram</span>
                    </a>
                    
                    <a href="https://www.facebook.com/profile.php?id=61584290236085" class="social-icon" target="_blank">
                        <div class="social-icon-circle">
                            <i class="fab fa-facebook-f"></i>
                        </div>
                        <span class="social-platform">Facebook</span>
                    </a>
                    
                    <a href="https://wa.me/919217045333" class="social-icon" target="_blank">
                        <div class="social-icon-circle">
                            <i class="fab fa-whatsapp"></i>
                        </div>
                        <span class="social-platform">WhatsApp</span>
                    </a>
                </div>
            </div>
        </section>
    </section>

    <!-- Footer -->
    <footer>
        <div class="container">
            <div class="footer-content">
                <div class="footer-column">
                    <h3>Studio's Salon Art</h3>
                    <p>Providing expert styling and total rejuvenation for men and women. We combine precision artistry with relaxation.</p>
                    <div class="contact-info">
                        <i class="fas fa-map-marker-alt"></i>
                        <div>
                            <p><strong>Location:</strong></p>
                            <p>GROUND FLOOR, 134/A, Navjivan Buddh Vihar</p>
                            <p>Taimoor Nagar, New Friends Colony</p>
                            <p>New Delhi, Delhi 110065</p>
                        </div>
                    </div>
                </div>
                
                <div class="footer-column">
                    <h3>Quick Links</h3>
                    <ul>
                        <li><a href="#home" class="nav-link" data-page="home">Home</a></li>
                        <li><a href="#about" class="nav-link" data-page="about">About Us</a></li>
                        <li><a href="#services" class="nav-link" data-page="services">Services</a></li>
                        <li><a href="#booking" class="nav-link" data-page="booking">Book Appointment</a></li>
                        <li><a href="#gallery" class="nav-link" data-page="gallery">Gallery</a></li>
                    </ul>
                </div>
                
                <div class="footer-column">
                    <h3>Contact Info</h3>
                    <div class="contact-info">
                        <i class="fas fa-phone-alt"></i>
                        <div>
                            <p><strong>Phone:</strong></p>
                            <p>9217045333</p>
                        </div>
                    </div>
                    <div class="contact-info">
                        <i class="fab fa-whatsapp"></i>
                        <div>
                            <p><strong>WhatsApp:</strong></p>
                            <p>+91 92170 45333</p>
                        </div>
                    </div>
                    <div class="contact-info">
                        <i class="fas fa-clock"></i>
                        <div>
                            <p><strong>Hours:</strong></p>
                            <p>Mon-Sat: 10AM-8PM</p>
                            <p>Sun: 11AM-6PM</p>
                        </div>
                    </div>
                </div>
                
                <div class="footer-column">
                    <h3>Follow Us</h3>
                    <p>Stay connected for latest offers and styling tips</p>
                    
                    <div class="footer-social">
                        <a href="https://www.instagram.com/studiossalaonart?igsh=NnQwcm5meWExZWhy&utm_source=qr" target="_blank" title="Instagram">
                            <i class="fab fa-instagram"></i>
                        </a>
                        <a href="https://www.facebook.com/profile.php?id=61584290236085" target="_blank" title="Facebook">
                            <i class="fab fa-facebook-f"></i>
                        </a>
                        <a href="https://wa.me/919217045333" target="_blank" title="WhatsApp">
                            <i class="fab fa-whatsapp"></i>
                        </a>
                    </div>
                    
                    <div class="booking-note" style="margin-top: 20px;">
                        <p><strong>Scratch & Win:</strong> Scratch the card on our homepage to reveal your exclusive discount code!</p>
                    </div>
                </div>
            </div>
            
            <div class="copyright">
                <p>© 2024 Studio's Salon Art. All Rights Reserved. | Style by Professionals | Feel the Beauty & Confidence</p>
                <p style="margin-top: 10px; font-size: 0.8rem;">
                    <a href="https://www.instagram.com/studiossalaonart?igsh=NnQwcm5meWExZWhy&utm_source=qr" target="_blank" style="color: var(--accent);">Instagram</a> | 
                    <a href="https://www.facebook.com/profile.php?id=61584290236085" target="_blank" style="color: var(--accent);">Facebook</a> | 
                    <a href="https://wa.me/919217045333" target="_blank" style="color: var(--accent);">WhatsApp</a>
                </p>
            </div>
        </div>
    </footer>

    <!-- Coupon Modal -->
    <div class="modal" id="couponModal">
        <div class="modal-content">
            <span class="close-modal">&times;</span>
            <h3>How to Use Your Coupon</h3>
            <p>To redeem your <strong id="discountAmount">15% discount</strong> using coupon code <span class="coupon-code" style="font-size: 1.2rem;" id="modalCouponCode">STUDIO15</span>:</p>
            <ol style="text-align: left; margin: 20px 0 20px 20px;">
                <li>Fill out the booking form on our website</li>
                <li>Enter your unique code in the "Coupon Code" field</li>
                <li>Submit your appointment request</li>
                <li>We'll confirm your booking and apply the discount</li>
                <li>Present the coupon code when you visit the salon</li>
            </ol>
            <p><strong>Terms & Conditions:</strong></p>
            <ul style="text-align: left; margin: 10px 0 20px 20px; font-size: 0.9rem;">
                <li>Valid for first-time clients only</li>
                <li>Cannot be combined with other offers</li>
                <li>Valid on services only (not products)</li>
                <li>Expires 30 days from generation</li>
                <li>Each visitor gets a unique code</li>
            </ul>
            <a href="#booking" class="cta-button nav-link" data-page="booking" style="display: inline-block; margin-top: 10px;">Book Now with Coupon</a>
        </div>
    </div>

    <script>
        // Mobile menu toggle
        const mobileMenuBtn = document.getElementById('mobileMenuBtn');
        const mainNav = document.getElementById('mainNav');
        
        mobileMenuBtn.addEventListener('click', () => {
            mainNav.classList.toggle('active');
            mobileMenuBtn.innerHTML = mainNav.classList.contains('active') 
                ? '<i class="fas fa-times"></i>' 
                : '<i class="fas fa-bars"></i>';
        });
        
        // Page navigation system
        const navLinks = document.querySelectorAll('.nav-link');
        const pages = document.querySelectorAll('.page');
        
        // Function to show a specific page and hide others
        function showPage(pageId) {
            // Hide all pages
            pages.forEach(page => {
                page.classList.remove('active');
            });
            
            // Show the selected page
            const activePage = document.getElementById(`${pageId}-page`);
            if (activePage) {
                activePage.classList.add('active');
            }
            
            // Update active nav link
            navLinks.forEach(link => {
                link.classList.remove('active');
                if (link.getAttribute('data-page') === pageId) {
                    link.classList.add('active');
                }
            });
            
            // Close mobile menu if open
            mainNav.classList.remove('active');
            mobileMenuBtn.innerHTML = '<i class="fas fa-bars"></i>';
            
            // Scroll to top of page
            window.scrollTo(0, 0);
        }
        
        // Add click event to all nav links
        navLinks.forEach(link => {
            link.addEventListener('click', function(e) {
                e.preventDefault();
                const pageId = this.getAttribute('data-page');
                showPage(pageId);
                
                // Update URL hash
                window.location.hash = `#${pageId}`;
            });
        });
        
        // Check URL hash on page load
        window.addEventListener('load', () => {
            const hash = window.location.hash.substring(1);
            if (hash && document.getElementById(`${hash}-page`)) {
                showPage(hash);
            } else {
                showPage('home');
            }
        });
        
        // Handle hash changes
        window.addEventListener('hashchange', () => {
            const hash = window.location.hash.substring(1);
            if (hash && document.getElementById(`${hash}-page`)) {
                showPage(hash);
            }
        });
        
        // Coupon modal functionality
        const couponModal = document.getElementById('couponModal');
        const howToUseBtn = document.getElementById('howToUseBtn');
        const closeModal = document.querySelector('.close-modal');
        
        howToUseBtn.addEventListener('click', () => {
            couponModal.style.display = 'flex';
        });
        
        closeModal.addEventListener('click', () => {
            couponModal.style.display = 'none';
        });
        
        window.addEventListener('click', (e) => {
            if (e.target === couponModal) {
                couponModal.style.display = 'none';
            }
        });
        
        // Booking form submission
        const bookingForm = document.getElementById('bookingForm');
        
        bookingForm.addEventListener('submit', function(e) {
            e.preventDefault();
            
            // Get form values
            const name = document.getElementById('name').value;
            const phone = document.getElementById('phone').value;
            const date = document.getElementById('date').value;
            const time = document.getElementById('time').value;
            const coupon = document.getElementById('coupon').value;
            
            // Get current coupon code from scratch card
            const currentCouponCode = document.getElementById('couponCodeDisplay').textContent;
            
            // Validate coupon code
            let couponMessage = '';
            if (coupon === currentCouponCode) {
                couponMessage = ' (discount applied)';
            } else if (coupon) {
                couponMessage = ' (invalid coupon code)';
            }
            
            // Create WhatsApp message
            const message = `Hi, I'd like to book an appointment at Studio's Salon Art.%0A%0AName: ${name}%0APhone: ${phone}%0ADate: ${date}%0ATime: ${time}%0ACoupon Code: ${coupon || 'None'}${couponMessage}`;
            
            // Open WhatsApp with pre-filled message
            window.open(`https://wa.me/919217045333?text=${message}`, '_blank');
            
            // Show confirmation
            alert(`Thank you ${name}! Your appointment request has been sent. We'll contact you shortly to confirm.`);
            
            // Reset form
            bookingForm.reset();
            // Keep the coupon code in the form
            document.getElementById('coupon').value = currentCouponCode;
        });
        
        // Set minimum date for booking to today
        const today = new Date().toISOString().split('T')[0];
        document.getElementById('date').min = today;
        
        // Auto-fill date with tomorrow as default
        const tomorrow = new Date();
        tomorrow.setDate(tomorrow.getDate() + 1);
        const tomorrowFormatted = tomorrow.toISOString().split('T')[0];
        document.getElementById('date').value = tomorrowFormatted;
        
        // Chat Bot Functionality
        const chatBotBtn = document.getElementById('chatBotBtn');
        const chatBotWindow = document.getElementById('chatBotWindow');
        const closeChatBtn = document.getElementById('closeChatBtn');
        const chatMessages = document.getElementById('chatMessages');
        const chatInput = document.getElementById('chatInput');
        const sendMessageBtn = document.getElementById('sendMessageBtn');
        const quickOptions = document.querySelectorAll('.quick-option');
        
        // Bot responses database
        const botResponses = {
            'greeting': [
                "Hello! Welcome to Studio's Salon Art. How can I assist you today?",
                "Hi there! I'm your Salon Assistant. What can I help you with?",
                "Welcome! I'm here to help you with information about our salon services."
            ],
            'services': "We offer a wide range of services including:\n• Hair Transformations (Coloring, Keratin, Cutting)\n• Skin Aesthetics (Facials, Waxing, Treatments)\n• Gentleman's Grooming\n• Exclusive Packages\n• Chemical & Color Work\n\nYou can view our complete services on the Services page.",
            'pricing': "Our pricing varies based on services:\n• Basic services start from ₹299*\n• Hair treatments from ₹2999*\n• Package deals from ₹999\n• Special offers available\n\n*Starting prices, final cost depends on hair length and specific requirements.",
            'booking': "You can book an appointment:\n1. Use our online booking form\n2. Call us at 9217045333\n3. WhatsApp us at +91 92170 45333\n4. Visit our salon directly\n\nWe recommend appointments to avoid waiting.",
            'hours': "Our opening hours are:\n• Monday - Saturday: 10:00 AM - 8:00 PM\n• Sunday: 11:00 AM - 6:00 PM\n\nWe're located at GROUND FLOOR, 134/A, Navjivan Buddh Vihar, Taimoor Nagar, New Friends Colony, New Delhi.",
            'coupon': "We have a scratch card coupon system on our homepage! Scratch to reveal your unique discount code. Each visitor gets a different code.",
            'location': "We're located at:\nGROUND FLOOR, 134/A, Navjivan Buddh Vihar\nTaimoor Nagar, New Friends Colony\nNew Delhi, Delhi 110065\n\nYou can find us on Google Maps or use the map on our Contact page.",
            'default': "I'm not sure I understand. You can ask me about:\n• Our services\n• Pricing\n• Booking appointments\n• Opening hours\n• Special offers\n• Our location\n\nOr use the quick options above!"
        };
        
        // Function to add a message to the chat
        function addMessage(text, isUser = false) {
            const messageDiv = document.createElement('div');
            messageDiv.className = `message ${isUser ? 'user-message' : 'bot-message'}`;
            messageDiv.textContent = text;
            chatMessages.appendChild(messageDiv);
            chatMessages.scrollTop = chatMessages.scrollHeight;
        }
        
        // Function to get a random greeting
        function getRandomGreeting() {
            const greetings = botResponses['greeting'];
            return greetings[Math.floor(Math.random() * greetings.length)];
        }
        
        // Function to handle user input
        function handleUserInput(input) {
            addMessage(input, true);
            
            // Process input after a short delay
            setTimeout(() => {
                let response = botResponses['default'];
                input = input.toLowerCase();
                
                if (input.includes('hello') || input.includes('hi') || input.includes('hey')) {
                    response = getRandomGreeting();
                } else if (input.includes('service') || input.includes('offer') || input.includes('do you have')) {
                    response = botResponses['services'];
                } else if (input.includes('price') || input.includes('cost') || input.includes('how much')) {
                    response = botResponses['pricing'];
                } else if (input.includes('book') || input.includes('appointment') || input.includes('schedule')) {
                    response = botResponses['booking'];
                } else if (input.includes('hour') || input.includes('open') || input.includes('close') || input.includes('time')) {
                    response = botResponses['hours'];
                } else if (input.includes('coupon') || input.includes('discount') || input.includes('offer') || input.includes('promo')) {
                    response = botResponses['coupon'];
                } else if (input.includes('location') || input.includes('address') || input.includes('where') || input.includes('find')) {
                    response = botResponses['location'];
                } else if (input.includes('thank') || input.includes('thanks')) {
                    response = "You're welcome! Is there anything else I can help you with?";
                } else if (input.includes('bye') || input.includes('goodbye')) {
                    response = "Thank you for chatting! Feel free to ask if you have more questions. Have a great day!";
                }
                
                // Split response by newlines for better formatting
                const responseLines = response.split('\n');
                responseLines.forEach(line => {
                    if (line.trim()) {
                        addMessage(line.trim());
                    }
                });
            }, 500);
        }
        
        // Toggle chat window
        chatBotBtn.addEventListener('click', () => {
            chatBotWindow.classList.toggle('active');
        });
        
        closeChatBtn.addEventListener('click', () => {
            chatBotWindow.classList.remove('active');
        });
        
        // Send message on button click
        sendMessageBtn.addEventListener('click', () => {
            const message = chatInput.value.trim();
            if (message) {
                handleUserInput(message);
                chatInput.value = '';
            }
        });
        
        // Send message on Enter key
        chatInput.addEventListener('keypress', (e) => {
            if (e.key === 'Enter') {
                const message = chatInput.value.trim();
                if (message) {
                    handleUserInput(message);
                    chatInput.value = '';
                }
            }
        });
        
        // Quick option buttons
        quickOptions.forEach(option => {
            option.addEventListener('click', () => {
                const question = option.getAttribute('data-question');
                let questionText = '';
                
                switch(question) {
                    case 'services': questionText = "What services do you offer?"; break;
                    case 'pricing': questionText = "What are your prices?"; break;
                    case 'booking': questionText = "How do I book an appointment?"; break;
                    case 'hours': questionText = "What are your opening hours?"; break;
                    case 'coupon': questionText = "Do you have any special offers?"; break;
                    case 'location': questionText = "Where are you located?"; break;
                }
                
                if (questionText) {
                    handleUserInput(questionText);
                }
            });
        });
        
        // Auto-open chat bot after 10 seconds on first visit
        setTimeout(() => {
            if (!localStorage.getItem('chatBotOpened')) {
                chatBotWindow.classList.add('active');
                localStorage.setItem('chatBotOpened', 'true');
            }
        }, 10000);
        
        // Hero Slider Functionality
        const sliderSlides = document.querySelectorAll('.slider-slide');
        const sliderDots = document.querySelectorAll('.slider-dot');
        const prevBtn = document.querySelector('.prev-slide');
        const nextBtn = document.querySelector('.next-slide');
        const autoplayBtn = document.getElementById('autoplayBtn');
        
        let currentSlide = 0;
        let slideInterval;
        let isAutoplay = true;
        const slideDuration = 5000; // 5 seconds
        
        // Initialize slider
        function initSlider() {
            // Start autoplay
            startAutoplay();
            
            // Set up event listeners
            prevBtn.addEventListener('click', showPrevSlide);
            nextBtn.addEventListener('click', showNextSlide);
            autoplayBtn.addEventListener('click', toggleAutoplay);
            
            // Dot navigation
            sliderDots.forEach(dot => {
                dot.addEventListener('click', function() {
                    const slideIndex = parseInt(this.getAttribute('data-slide'));
                    goToSlide(slideIndex);
                });
            });
            
            // Pause on hover
            const sliderContainer = document.querySelector('.slider-container');
            sliderContainer.addEventListener('mouseenter', pauseAutoplay);
            sliderContainer.addEventListener('mouseleave', resumeAutoplay);
        }
        
        // Show next slide
        function showNextSlide() {
            currentSlide = (currentSlide + 1) % sliderSlides.length;
            updateSlider();
        }
        
        // Show previous slide
        function showPrevSlide() {
            currentSlide = (currentSlide - 1 + sliderSlides.length) % sliderSlides.length;
            updateSlider();
        }
        
        // Go to specific slide
        function goToSlide(slideIndex) {
            currentSlide = slideIndex;
            updateSlider();
        }
        
        // Update slider display
        function updateSlider() {
            // Hide all slides
            sliderSlides.forEach(slide => {
                slide.classList.remove('active');
            });
            
            // Remove active class from all dots
            sliderDots.forEach(dot => {
                dot.classList.remove('active');
            });
            
            // Show current slide
            sliderSlides[currentSlide].classList.add('active');
            sliderDots[currentSlide].classList.add('active');
            
            // Reset autoplay timer
            resetAutoplay();
        }
        
        // Autoplay functions
        function startAutoplay() {
            if (isAutoplay) {
                slideInterval = setInterval(showNextSlide, slideDuration);
                autoplayBtn.innerHTML = '<i class="fas fa-pause"></i>';
            }
        }
        
        function pauseAutoplay() {
            clearInterval(slideInterval);
        }
        
        function resumeAutoplay() {
            if (isAutoplay) {
                startAutoplay();
            }
        }
        
        function resetAutoplay() {
            pauseAutoplay();
            resumeAutoplay();
        }
        
        function toggleAutoplay() {
            isAutoplay = !isAutoplay;
            if (isAutoplay) {
                startAutoplay();
                autoplayBtn.innerHTML = '<i class="fas fa-pause"></i>';
            } else {
                pauseAutoplay();
                autoplayBtn.innerHTML = '<i class="fas fa-play"></i>';
            }
        }
        
        // Initialize slider on page load
        window.addEventListener('load', initSlider);
        
        // Scratch Card Functionality
        const scratchCanvas = document.getElementById('scratchCanvas');
        const scratchArea = document.getElementById('scratchArea');
        const couponReveal = document.getElementById('couponReveal');
        const couponCodeDisplay = document.getElementById('couponCodeDisplay');
        const revealAllBtn = document.getElementById('revealAllBtn');
        const resetScratchBtn = document.getElementById('resetScratchBtn');
        const modalCouponCode = document.getElementById('modalCouponCode');
        const discountAmount = document.getElementById('discountAmount');
        
        // Generate a random coupon code
        function generateCouponCode() {
            const prefixes = ['STUDIO', 'SALON', 'ART', 'BEAUTY', 'STYLE', 'GLAM'];
            const suffixes = ['15', '20', '25', '30', 'OFF', 'SAVE'];
            const numbers = Math.floor(Math.random() * 1000).toString().padStart(3, '0');
            
            const randomPrefix = prefixes[Math.floor(Math.random() * prefixes.length)];
            const randomSuffix = suffixes[Math.floor(Math.random() * suffixes.length)];
            
            // Random discount between 10% and 25%
            const discount = Math.floor(Math.random() * 16) + 10; // 10-25%
            
            // Different code patterns
            const patterns = [
                `${randomPrefix}${discount}${randomSuffix}`,
                `${randomPrefix}${numbers}${randomSuffix}`,
                `${discount}%${randomPrefix}${numbers}`,
                `${randomPrefix}${discount}OFF${numbers}`,
                `SAVE${discount}${randomPrefix}`
            ];
            
            const couponCode = patterns[Math.floor(Math.random() * patterns.length)];
            
            // Update discount amount display
            const discountElements = document.querySelectorAll('.discount-amount');
            discountElements.forEach(el => {
                el.textContent = `${discount}% Discount Coupon`;
            });
            
            // Update modal
            modalCouponCode.textContent = couponCode;
            discountAmount.textContent = `${discount}% discount`;
            
            // Update booking form
            document.getElementById('coupon').value = couponCode;
            
            return couponCode;
        }
        
        // Initialize canvas for scratch card
        let isDrawing = false;
        let lastX = 0;
        let lastY = 0;
        const ctx = scratchCanvas.getContext('2d');
        
        // Set canvas size
        function setCanvasSize() {
            const rect = scratchArea.getBoundingClientRect();
            scratchCanvas.width = rect.width;
            scratchCanvas.height = rect.height;
            
            // Draw scratch surface
            ctx.fillStyle = '#cccccc';
            ctx.fillRect(0, 0, scratchCanvas.width, scratchCanvas.height);
            ctx.globalCompositeOperation = 'destination-out';
        }
        
        // Initialize scratch card
        function initScratchCard() {
            setCanvasSize();
            
            // Generate new coupon code
            const couponCode = generateCouponCode();
            couponCodeDisplay.textContent = couponCode;
            
            // Hide the coupon reveal initially
            couponReveal.style.opacity = '0';
            
            // Event listeners for scratching
            scratchCanvas.addEventListener('mousedown', startDrawing);
            scratchCanvas.addEventListener('mousemove', draw);
            scratchCanvas.addEventListener('mouseup', stopDrawing);
            scratchCanvas.addEventListener('mouseout', stopDrawing);
            
            // Touch events for mobile
            scratchCanvas.addEventListener('touchstart', startDrawingTouch);
            scratchCanvas.addEventListener('touchmove', drawTouch);
            scratchCanvas.addEventListener('touchend', stopDrawing);
        }
        
        // Drawing functions
        function startDrawing(e) {
            isDrawing = true;
            [lastX, lastY] = [e.offsetX, e.offsetY];
        }
        
        function draw(e) {
            if (!isDrawing) return;
            
            ctx.lineWidth = 25;
            ctx.lineCap = 'round';
            ctx.lineJoin = 'round';
            
            ctx.beginPath();
            ctx.moveTo(lastX, lastY);
            ctx.lineTo(e.offsetX, e.offsetY);
            ctx.stroke();
            
            [lastX, lastY] = [e.offsetX, e.offsetY];
            
            // Check if enough has been scratched
            checkScratchedArea();
        }
        
        function stopDrawing() {
            isDrawing = false;
        }
        
        // Touch events
        function startDrawingTouch(e) {
            e.preventDefault();
            const touch = e.touches[0];
            const rect = scratchCanvas.getBoundingClientRect();
            isDrawing = true;
            [lastX, lastY] = [touch.clientX - rect.left, touch.clientY - rect.top];
        }
        
        function drawTouch(e) {
            e.preventDefault();
            if (!isDrawing) return;
            
            const touch = e.touches[0];
            const rect = scratchCanvas.getBoundingClientRect();
            const x = touch.clientX - rect.left;
            const y = touch.clientY - rect.top;
            
            ctx.lineWidth = 30;
            ctx.lineCap = 'round';
            ctx.lineJoin = 'round';
            
            ctx.beginPath();
            ctx.moveTo(lastX, lastY);
            ctx.lineTo(x, y);
            ctx.stroke();
            
            [lastX, lastY] = [x, y];
            
            // Check if enough has been scratched
            checkScratchedArea();
        }
        
        // Check how much of the canvas has been scratched
        function checkScratchedArea() {
            const imageData = ctx.getImageData(0, 0, scratchCanvas.width, scratchCanvas.height);
            const pixels = imageData.data;
            let scratchedPixels = 0;
            let totalPixels = 0;
            
            // Count transparent pixels (scratched area)
            for (let i = 3; i < pixels.length; i += 4) {
                if (pixels[i] === 0) {
                    scratchedPixels++;
                }
                totalPixels++;
            }
            
            const scratchedPercentage = (scratchedPixels / totalPixels) * 100;
            
            // If 50% or more is scratched, reveal the coupon
            if (scratchedPercentage >= 50) {
                revealCoupon();
            }
            
            // Fade in the coupon as more is scratched
            const opacity = Math.min(1, scratchedPercentage / 30);
            couponReveal.style.opacity = opacity.toString();
        }
        
        // Reveal the coupon
        function revealCoupon() {
            couponReveal.style.opacity = '1';
            
            // Show confetti effect
            showConfetti();
            
            // Update modal with current coupon code
            modalCouponCode.textContent = couponCodeDisplay.textContent;
        }
        
        // Show confetti effect
        function showConfetti() {
            const confettiCount = 50;
            const scratchRect = scratchArea.getBoundingClientRect();
            
            for (let i = 0; i < confettiCount; i++) {
                const confetti = document.createElement('div');
                confetti.style.position = 'absolute';
                confetti.style.width = '10px';
                confetti.style.height = '10px';
                confetti.style.backgroundColor = getRandomColor();
                confetti.style.borderRadius = '50%';
                confetti.style.left = `${Math.random() * scratchRect.width}px`;
                confetti.style.top = `${Math.random() * scratchRect.height}px`;
                confetti.style.zIndex = '3';
                confetti.style.animation = `confettiFall ${1 + Math.random() * 2}s ease-out forwards`;
                
                scratchArea.appendChild(confetti);
                
                // Remove confetti after animation
                setTimeout(() => {
                    confetti.remove();
                }, 3000);
            }
            
            // Add CSS for confetti animation
            if (!document.getElementById('confetti-style')) {
                const style = document.createElement('style');
                style.id = 'confetti-style';
                style.textContent = `
                    @keyframes confettiFall {
                        0% { transform: translateY(0) rotate(0deg); opacity: 1; }
                        100% { transform: translateY(${scratchRect.height}px) rotate(360deg); opacity: 0; }
                    }
                `;
                document.head.appendChild(style);
            }
        }
        
        // Get random color for confetti
        function getRandomColor() {
            const colors = ['#8a6d5b', '#d4b8a7', '#f5e8dd', '#6d5546', '#E4405F', '#1877F2', '#25D366'];
            return colors[Math.floor(Math.random() * colors.length)];
        }
        
        // Reveal all button
        revealAllBtn.addEventListener('click', () => {
            ctx.clearRect(0, 0, scratchCanvas.width, scratchCanvas.height);
            revealCoupon();
        });
        
        // Reset scratch card
        resetScratchBtn.addEventListener('click', () => {
            // Clear canvas
            ctx.globalCompositeOperation = 'source-over';
            ctx.fillStyle = '#cccccc';
            ctx.fillRect(0, 0, scratchCanvas.width, scratchCanvas.height);
            ctx.globalCompositeOperation = 'destination-out';
            
            // Hide coupon reveal
            couponReveal.style.opacity = '0';
            
            // Generate new coupon code
            generateCouponCode();
            couponCodeDisplay.textContent = generateCouponCode();
            
            // Update modal
            modalCouponCode.textContent = couponCodeDisplay.textContent;
        });
        
        // Handle window resize
        window.addEventListener('resize', setCanvasSize);
        
        // Initialize scratch card on page load
        window.addEventListener('load', initScratchCard);
    </script>
</body>
</html>
